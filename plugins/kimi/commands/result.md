---
description: Show the final output of a finished Kimi job (review JSON, diff, or both).
argument-hint: "[<job-id>] [--json] [--diff]"
---

```
node "${CLAUDE_PLUGIN_ROOT}/scripts/runner.mjs" result $ARGUMENTS
```

Return the runner's stdout as-is.
